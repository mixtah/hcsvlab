- if !Rails.env.test?
  .row-fluid.clearfix.environment
    .span12
      - if Rails.env.development?
        #dev
        %p= "#{PROJECT_NAME} Env: Development."
      - else
        %p
          Developed by Alveo Virtual Laboratory.  Version:
          = render 'shared/tag'
  

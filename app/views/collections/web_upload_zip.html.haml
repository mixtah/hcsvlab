%h2 Upload Zip
= form_tag({controller: 'collections', action: 'web_upload_zip'}, method: 'post', :id => 'upload_zip_form', multipart: true) do

  .intro
    Uploading a zip file allows you to import many items and documents in one step.
    <a href="#TODO">See here</a> for help with the structure of your zip file.

  = label_tag :zip_file, 'File:'
  = file_field_tag 'zip_file', :class => ""

  = render 'display_additional_metadata'

  .form-actions
    = submit_tag 'Upload', :class => "btn btn-primary"
    = link_to 'Cancel', collection_path(@collection.name), :class => "btn"

= render 'shared/nectar_attribution'
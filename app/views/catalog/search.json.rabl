node(:num_results) { |x| @response["response"]["numFound"] }
node(:items) { @response["response"]["docs"].collect { |item| catalog_url(item[MetadataHelper.short_form(MetadataHelper::COLLECTION)].first, item.slice("DC_identifier_tesim", "DCTERMS_identifier_tesim", "dc_identifier_tesim").first.last) } }